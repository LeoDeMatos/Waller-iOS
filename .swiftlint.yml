excluded: # paths to ignore during linting. Takes precedence over `included`.
    - Carthage
    - Pods
    - Mineirao/AppDelegate.swift
    - R.generated.swift

disabled_rules:
    - trailing_whitespace
    - force_cast
    - empty_parameters
    - identifier_name
